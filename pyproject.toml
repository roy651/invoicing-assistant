[project]
name = "invoicing-assistant"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "gspread>=6.0",
    "google-auth>=2.0",
    "google-auth-oauthlib>=1.0",
]

[dependency-groups]
dev = [
    "pre-commit>=4.0",
]
